<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.test.mappers.reply">

		<!-- 댓글 조회 -->
		<select id="replyList" parameterType="int" resultType="com.board.domain.ReplyVO">
			select
				rno,no, rpassword, content, replyWirter
			from reply
				where no = #{no}		
		</select>
		
		<!-- 댓글 작성 -->
		<insert id="replyWrite" parameterType="com.board.domain.ReplyVO">
			insert into reply(no, rpassword, content, replyWirter)
			
				value(#{no}, #{rpassword}, #{content}, #{replyWirter})
				
		</insert>
			
		<!-- 댓글 수정 -->
		<update id="replyModify" parameterType="com.board.domain.ReplyVO">
			update reply set
				content = #{content}
			where rno = #{rno}
				and no = #{no}	
		</update>
		
		<!-- 댓글 삭제 -->
		<delete id="replyDelete" parameterType="com.board.domain.ReplyVO">
			delete from reply
			where rno = #{rno}
			and no = #{no}
				
		</delete>
		
		<!-- 특정 댓글 조회 -->
		<select id="readReplySelect" resultType="com.board.domain.ReplyVO">
		 select
		     no, rno, content, rpassword
		 from reply
		     where rno = #{rno}
		     		
		</select>
		
	
	

</mapper>